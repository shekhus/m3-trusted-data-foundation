# Build Plan — M3 Trusted Data Foundation (Project A)

**Status:** Build plan v1 + Addendum 1 (`docs/addendum1.md`) · **Date:** 12 September 2026

Public copy of the governing plan. All data and the customer are fictional; engagement-specific
details are deliberately excluded. Project B (`m3-supply-chain-command-center`) has its own copy.

---

## 0. Read This First

### 0.1 What these two projects are

| | Project A | Project B |
|---|---|---|
| **Name** | M3 Trusted Data Foundation | M3 Supply-Chain Command Center |
| **Repo** | `m3-trusted-data-foundation` | `m3-supply-chain-command-center` |
| **Playbook pattern** | Project 4 (data onboarding & integration) as the core, with Project 1 (metadata + access level) and Project 3 (deterministic control) woven in | Project 5 (operations command center) as the core, with Project 2 (action + human approval + policy code) and Project 1 (permission filter before the model) woven in |
| **One-line pitch** | "Two BI tools, one truth: AI proposes the mappings, code validates every record, people confirm, and drift is caught before the report breaks." | "Every morning: what changed, why, what to do next — code computes, AI narrates with citations, a person approves before anything is sent." |
| **Ships as** | FastAPI + Postgres + Streamlit portal, deployed on Railway, Dockerised, CI-tested, with an evaluation report | FastAPI + Postgres + LangGraph approval flow + Streamlit console, deployed on Railway with a daily cron, Dockerised, CI-tested, with a replay-based evaluation report |
| **Relationship** | Produces the gold tables | Consumes them — but ships with its own synthetic gold so it stands alone |

Together they exercise **all five** FDE project patterns from the playbook, while being two clearly different engineering disciplines (data integration vs. analytics + agentic action loop). That is deliberate: it lets you answer "show me integration work" and "show me agentic work" with two different repos.

### 0.2 Public-only rule (non-negotiable)

Everything in this repo is public: no client names, no sector + geography + metric combinations, no verbatim source numbers.

- The customer is **"Prairie Bend Foods"** — a fictional multi-plant North American protein processor on Infor M3.
- All data is synthetic, generated by code (`synth/`).
- The seam for real data is the source adapter (`sources/synthetic/` vs. a future `sources/m3_ion/`). The rest of the codebase does not change.

Checks before anything goes public:
1. Confirm "Prairie Bend Foods" isn't a real company (`scripts/check_company_name.py` from the generator lists the searches).
2. Review the public customer profile wording before publishing.
3. `tests/test_repo_hygiene.py` must pass — it fails if a banned engagement term appears in any tracked file.

### 0.3 Honesty boundaries baked into the plan

- Both projects run on **synthetic data modelled on Infor M3 structures**. The README says so in the first paragraph.
- The M3 table names used (OOHEAD, OOLINE, MITMAS, OCUSMA, MITBAL, MITLOC, MITBAT) are the commonly documented M3 table names. **Physical field names are not asserted anywhere** — canonical names are used, and the README says "field-level mapping to M3 physical columns to be confirmed with an M3 SME."
- Every metric target in this plan is an **acceptance criterion you set for yourself**, not a claim about client outcomes.
- The reference repo you shared (`cold-chain-logistics-FDE-Project`) is cited in the README as an influence for the "permissions in the database, not the prompt" pattern. Credit given, nothing copied.

### 0.4 Timeline at a glance (indicative — evening/weekend pace)

| Weeks | Project | Milestone |
|---|---|---|
| 1–6 | A | Foundation shipped: portal, validation, exception queue, drift, reconciliation, retrieval, exception agent, evals, deployed (six weeks per Addendum 1) |
| 7–10 | B | Command center shipped: detectors, grounded brief, approval flow, Jira adapter, replay evals, deployed |
| 11 | Both | Demo videos, README polish, public-profile review, portfolio site cards |

Target completion: **late November 2026**. If time gets tight, Project A alone, done completely, is a stronger portfolio than both half-done (Playbook §4).

---

## 1. Shared Foundations (build once, use in both)

### 1.1 Stack — deliberately boring

| Layer | Choice | Why this and not something fancier |
|---|---|---|
| Language | Python 3.12 | Your production language |
| API | FastAPI + Pydantic v2 | Typed contracts, OpenAPI for free |
| Storage | PostgreSQL (Railway) with `bronze`, `silver`, `gold`, `ops` schemas | Medallion semantics without needing Databricks to run locally; a Delta adapter is a documented "before production" step |
| Data processing | pandas (or polars if you prefer) | Profiling and validation at portfolio scale |
| LLM | One provider behind `llm/client.py`; default Anthropic Claude Sonnet, swap via env | One provider, one adapter. Not five. |
| UI | Streamlit | Fast, you know it, honest about being an internal console not a product UI |
| Orchestration | **A: none** (fixed pipeline, plain Python). **B: LangGraph** with a Postgres checkpointer and `interrupt_before` on the execute step | This contrast is an interview asset — see §4.3 |
| Scheduling | Railway cron service (or GitHub Actions `schedule`) hitting an authenticated endpoint | No Airflow for one daily job |
| Observability | Structured JSON logs + `ops.llm_calls`, `ops.tool_calls`, `ops.runs` tables + Streamlit ops page + `/healthz` | Queryable, no extra vendor |
| Tests / evals | pytest; `make eval` writes `evals/results/<date>.json` and `evals/REPORT.md` | Evals are code, committed, reproducible |
| CI/CD | GitHub Actions: lint → tests → evals on sample → Docker build → deploy to Railway on tag | Gate before deploy (the reference repo had none) |
| Local | Docker Compose (Postgres + app) | Anyone can run it |

**Verify before pinning:** current package names and versions for `langgraph`, the LangGraph Postgres checkpointer (I believe it is `langgraph-checkpoint-postgres`, but check), `anthropic`, `streamlit`, `fastapi`. Do not copy versions from the reference repo — several looked implausible and I could not verify them.

### 1.2 Windows / Claude Code CLI conventions (from your environment notes)

- Repos live **outside OneDrive** (Docker volume compatibility).
- CMD cannot run multi-line Python; every operation is a script under `scripts/` or a `make` target (use `make` via Git Bash, or a `tasks.py` with `invoke` if `make` is painful on Windows — pick one and record it in CLAUDE.md).
- Multi-root VS Code workspace: both repos + the playbook folder.
- Each repo has a `CLAUDE.md` (supplied) that Claude Code reads first.

### 1.3 The synthetic data generator (`synth/`) — shared design, two instances

This is the most important shared component. Its quality determines whether your evaluations mean anything, because **the generator knows the truth** and writes it to `ground_truth/`.

**Fictional world:** Prairie Bend Foods. 3 plants (PLT-01, PLT-02, PLT-03), 2 distribution warehouses, ~30 customers (retail, foodservice, distributor), ~80 items in product groups (ground, primals, case-ready, value-added, offal), catch-weight items flagged. 18 months of daily data (Mar 2025 – Aug 2026). Deterministic seed.

**Tables generated (canonical names; M3 analogue noted for the README):**

| Canonical table | M3 analogue | Grain | Key fields |
|---|---|---|---|
| `customer_orders` | OOHEAD / OOLINE | order line | order_no, line_no, customer_no, item_no, plant, requested_date, confirmed_delivery_date, ordered_qty, ordered_weight_lb, uom |
| `deliveries` | delivery / invoice lines | order line | order_no, line_no, issue_date, invoiced_qty, invoiced_weight_lb, lot_no |
| `items` | MITMAS | item | item_no, item_name, product_group, catch_weight_flag, shelf_life_days, std_yield_pct |
| `customers` | OCUSMA | customer | customer_no, customer_name, city, state, channel |
| `inventory_snapshots` | MITBAL / MITLOC / MITBAT | location × lot × day | snapshot_date, location, item_no, lot_no, on_hand_lb, production_date, expiry_date |
| `production_yield` | production/yield reporting | plant × line × product_group × day | run_date, plant, line, product_group, input_lb, output_lb, std_yield_pct |
| `delivery_performance_report` | a 20-column delivery performance report feed | order line | Company Name, Customer Number, … , Ordered Qty, Invoiced Qty, Case Fill Rate, On Time |

**Per-plant quirks (Project A must survive these):**

| Plant | Date format | Weight unit | Customer no. format | Other |
|---|---|---|---|---|
| PLT-01 | ISO `2026-03-04` | lb | `C000123` | clean |
| PLT-02 | US `03/04/2026` | **kg** | `123` (no prefix) | 1% missing weights |
| PLT-03 | Excel serial `46085` | lb | `CUST-0123` | 0.5% duplicated lines, item numbers with trailing spaces |

**Seeded faults (for Project A recall evals), written to `ground_truth/faults.json` with fault_id, table, row key, fault_type:** missing required field, ship date before order date, invoiced weight > 105% of ordered, unknown customer, unknown item, lot shipped after expiry, negative quantity, duplicate line, UOM mismatch.

**Seeded anomalies (for Project B detection evals), written to `ground_truth/anomalies.json` with anomaly_id, metric, segment, start_date, end_date, cause, magnitude:**

| # | Metric | Segment | When | Planted cause |
|---|---|---|---|---|
| A1 | OTIF | lane PLT-02 → retail customer C000031 | 3 weeks, month 7 | late issue dates from PLT-02 |
| A2 | Fill rate (weight) | product group "case-ready" at PLT-01 | 2 weeks, month 9 | short weights |
| A3 | Yield variance | PLT-03 line L2, primals | 10 days, month 11 | output_lb depressed |
| A4 | Inventory age | warehouse DC-East, two lots | month 14 | lots not moving toward expiry |
| A5 | OTIF | all plants | 4 days, month 16 | holiday dip — **a decoy** the detector should down-weight via seasonality |
| A6 | Backlog | PLT-02 | month 17 | order surge |

Plus **noise anomalies** below threshold to test precision.

**Schema drift events (for Project A drift evals), `ground_truth/drift.json`:** month 12, PLT-02 renames `cust_no` → `customer_number`; month 15, PLT-01 adds a `lot_no` column and the business declares it required (this is the **mid-project change request** from Playbook §9.4).

### 1.4 Definition of "production-level" used throughout

A project is production-level for portfolio purposes when all of the following are true:

1. Deployed at a public URL with `/healthz` and auth on write endpoints.
2. `docker compose up` runs it locally from a clean clone in under five minutes.
3. CI runs lint, tests, and sample evals on every push; deploy is gated on green.
4. `evals/REPORT.md` exists, is generated by code, and includes failure analysis.
5. `RUNBOOK.md` lets another person operate it without you.
6. README answers the six playbook questions (Appendix B).
7. Every consequential action has a human approval gate; nothing writes to a system of record automatically.
8. Secrets are in env only; `.env.example` is committed; nothing sensitive in git history.

---

## 2. Project A — M3 Trusted Data Foundation

### 2.1 Discovery brief — PUBLIC version (Playbook Appendix A)

| Field | Entry |
|---|---|
| **Customer** | Prairie Bend Foods (fictional): multi-plant North American protein processor, ~1,200 employees, Infor M3 ERP |
| **Users** | Supply-chain analysts (2), BI developer (1), plant operations leads (3), VP Supply Chain (consumer of reports) |
| **Current workflow** | M3 extracts feed two BI tools. Tool 1 (a legacy report suite) has hand-maintained SQL per report. Tool 2 (a newer dashboard tool) rebuilds metrics independently. A shared inbox receives "this report looks wrong" tickets; an analyst reconciles by hand in a spreadsheet. |
| **Pain point** | The two tools report different OTIF for the same week; refreshes break silently when an M3 extract changes; bad rows (missing weights, duplicate lines) skew totals. Roughly a third of BI tickets are data-definition disputes rather than genuine report bugs. *(Illustrative framing for the fictional customer; no real ticket data.)* |
| **Systems involved** | Infor M3 (orders, deliveries, items, customers, inventory), plant scale/WMS exports, two BI tools, a ticketing system |
| **Constraints** | Read-only access to M3; no changes to existing reports during pilot; plant data varies in format; catch-weight items measured by count and weight; audit trail required for any definition change |
| **Success metric** | Both BI tools show the same OTIF and fill rate for any period; zero silent refresh failures; every excluded row has a reason and an owner; a new source or field is absorbed without rewriting reports |

### 2.3 Architecture

```
                 ┌──────────────────────────────────────────────────────────┐
  Sources        │  sources/                                                │
  (adapters)     │   synthetic/   m3_extract (CSV per plant)                │
                 │                report_feed (20-col delivery report)      │
                 │                plant_scale (mock REST, catch weights)    │
                 │   m3_ion/      (future: real M3 adapter — same interface)│
                 └───────────────┬──────────────────────────────────────────┘
                                 │  POST /ingest/{source}  (Idempotency-Key)
                                 ▼
   ┌──────────────┐    ┌──────────────┐    ┌──────────────┐    ┌──────────────┐
   │ 1 PROFILE    │───▶│ 2 MAP        │───▶│ 3 VALIDATE   │───▶│ 4 PUBLISH    │
   │ pandas stats │    │ LLM proposes │    │ rules in code│    │ gold tables  │
   │ fingerprint  │    │ human confirms│   │ exception Q  │    │ consumer views│
   │ drift check  │    │ versioned    │    │ lineage      │    │ reconcile    │
   └──────────────┘    └──────────────┘    └──────────────┘    └──────────────┘
          │                    │                   │                   │
          ▼                    ▼                   ▼                   ▼
   bronze.raw_*        ops.mapping_versions   silver.* + ops.exceptions   gold.* + ops.reconciliation
                                 ▲
                       Streamlit portal: upload → findings → confirm mapping → exception queue → drift alerts
```

**Metric dictionary** (`metrics/*.yaml`, versioned, compiled to SQL) sits beside the pipeline and is the single definition both consumer views use.

### 2.4 Features

| # | Feature | Implementation | Playbook principle |
|---|---|---|---|
| A1 | **Source adapters** | `SourceAdapter` protocol: `discover() → fetch(batch) → fingerprint()`. Three synthetic adapters ship; interface is the seam for a real M3 adapter | §9.2 |
| A2 | **Profiling** | Per column: dtype guess, null %, distinct %, min/max, date-format detection, unit inference (lb/kg by magnitude + header), duplicate-key rate. Emits `findings.md` + JSON | §9.2 |
| A3 | **Metric dictionary** | YAML per metric with `version`, `grain`, `definition`, `owner`, `changed_by`, `effective_from`. Compiler renders SQL views. Example below | §2.1 discovery brief |
| A4 | **AI-assisted mapping** | LLM receives: source profile + canonical schema + past confirmed mappings for similar sources. Returns JSON `{source_col → canonical_col, confidence, rationale, transform}` validated by Pydantic. **Heuristic fallback** (name similarity + dtype + value patterns) runs first and always; LLM improves on it. Human confirms in portal. Every mapping is a row in `ops.mapping_versions` | §9.3 — AI proposes, never silently rewrites |
| A5 | **Validation rules** | `rules/*.py`, each with id, severity, message, suggested fix. Run in code after mapping. Rule ids V001–V012 (below) | §7.3A, §8.3 — deterministic control outside the model |
| A6 | **Exception queue** | `ops.exceptions(exception_id, batch_id, rule_id, row_key, reason, suggested_fix, owner, status)`. Portal view with assign / resolve / re-run. Nothing dropped silently | §9.2 |
| A7 | **Idempotent ingest API** | `Idempotency-Key` header stored with request hash; replay returns the original result; batch_id per run; safe re-run after failure | §9.3 hardening |
| A8 | **Lineage** | `ops.lineage(gold_table, gold_col, silver_col, mapping_version, source, source_col)` — lets drift name exactly which report breaks | §9.3 |
| A9 | **Drift detection** | Fingerprint = hash of sorted `(col, dtype, null_bucket, pattern_class)`. On change: diff → walk lineage → produce "mapping M-0042 for PLT-02 `cust_no` broken; affects gold.fact_delivery, consumer views: otif_v3" → block publish + alert | §9.3 |
| A10 | **Reconciliation** | Two consumer views computed independently from gold via the compiled dictionary + one **legacy view** using the old definition on purpose. Nightly job compares OTIF/fill rate across all three per period → `ops.reconciliation` with deltas; portal shows green/red | §2.1 discovery brief |
| A11 | **Change absorption** | The month-15 "lot_no becomes required" event is handled by adding a canonical column + a rule + a dictionary bump — no report rewrite. Time it; report it | §9.4 |
| A12 | **Access levels** | Portal roles: `viewer`, `analyst` (can propose), `owner` (can confirm mapping, resolve exceptions). Enforced server-side | Project 1 §6.3 |

**Metric dictionary example (`metrics/otif.yaml`):**

```yaml
metric: otif
version: 3
owner: supply_chain_analytics
effective_from: 2026-09-01
grain: order_line
inputs:
  on_time:  "issue_date <= confirmed_delivery_date"
  in_full_count:  "invoiced_qty >= ordered_qty"
  in_full_weight: "invoiced_weight_lb >= ordered_weight_lb * (1 - tolerance_weight)"
params:
  tolerance_weight: 0.02          # catch-weight items
rules:
  in_full: "in_full_weight if catch_weight_flag else in_full_count"
  otif:    "on_time and in_full"
aggregations:
  otif_rate: "sum(otif) / count(*)"
notes: "v3 switched catch-weight items from count to weight basis. v2 used count for all items."
```

**Validation rule set (deterministic, in code):**

| Rule | Check | Severity |
|---|---|---|
| V001 | Required fields present (order_no, line_no, customer_no, item_no, plant, dates, ordered_qty) | block |
| V002 | issue_date ≥ order date; confirmed_delivery_date ≥ requested_date − 30d | block |
| V003 | invoiced_weight_lb within [0, 1.05 × ordered_weight_lb] | block |
| V004 | customer_no exists in customers (after normalisation) | block |
| V005 | item_no exists in items (after trim) | block |
| V006 | lot_no exists and expiry_date ≥ issue_date | block |
| V007 | quantities ≥ 0 | block |
| V008 | duplicate (order_no, line_no) within batch | block (keep first, queue rest) |
| V009 | UOM consistent with item master; kg converted to lb with source recorded | warn (auto-fix, logged) |
| V010 | date parse succeeded with one detected format per source | block |
| V011 | ordered_qty > 0 when invoiced_qty > 0 | warn |
| V012 | lot_no required (activated month 15 by dictionary change) | block |

### 2.5 Evaluation plan (`evals/`)

| Measure | Method | Target (your acceptance criterion) |
|---|---|---|
| **Mapping accuracy** | Compare proposed mapping vs `ground_truth/mappings.json`, per source; report heuristic-only vs LLM-assisted vs after-human | Top-1 ≥ 90% LLM-assisted; report human correction rate |
| **Validation coverage** | % of silver rows with all applicable rules executed | 100% (hard gate) |
| **Exception recall / precision** | Seeded faults caught vs missed; false exceptions on clean rows | Recall ≥ 95%, precision ≥ 95% |
| **Reconciliation** | Delta between the two consumer views | 0.00 for every period; legacy view delta reported as the "before" |
| **Drift detection** | Seeded drift events detected; correct mapping + reports named | 2/2 detected, correct lineage |
| **Change absorption** | Files touched and minutes to absorb the lot_no requirement | Report honestly; no report SQL edited |
| **Idempotency** | Replay the same ingest 3× | Row counts unchanged |
| **Onboarding time** | Wall clock from new source upload to gold publish | Report |

Failure analysis section: for every missed fault or wrong mapping, one line — what happened, why, what changed.

### 2.6 Failure cases to demonstrate (Playbook Appendix C)

| Case | Expected behaviour |
|---|---|
| Source unreachable | Ingest returns 503 with retry-after; batch marked `failed`; no partial publish |
| Stale source (file older than SLA) | Batch accepted but flagged `stale`; gold publish blocked; alert |
| LLM returns invalid JSON / hallucinated column | Pydantic rejects; heuristic mapping used; item flagged "needs review" in portal |
| Duplicate ingest (retry) | Same Idempotency-Key → original result; zero duplicate rows |
| Schema drift | Publish blocked; drift alert names mapping + affected reports |
| Mid-project new required field | Absorbed by dictionary + rule; exceptions queued for historical rows lacking it |
| Analyst tries to confirm a mapping | 403; only `owner` can confirm |

### 2.7 Five-week build plan

| Week | Build | Definition of done |
|---|---|---|
| **1** | Discovery briefs (both). Repo scaffold, Docker Compose, Postgres schemas. `synth/` generator + `ground_truth/`. Profiling module + `findings.md` | `make synth` produces all tables + truth files; `make profile` emits findings for all three sources; tests green |
| **2** | Metric dictionary + compiler. Heuristic mapper. LLM mapper + Pydantic contract. `ops.mapping_versions`. Portal: upload → findings → propose → confirm | A confirmed mapping produces silver tables for PLT-01; mapping eval runs |
| **3** | Rules engine V001–V011. Exception queue + portal view. Idempotent `/ingest`. Lineage table | All three plants through to silver; exception recall eval ≥ target; replay test passes |
| **4** | Drift detection. Gold publish + two consumer views + legacy view. Reconciliation job. Execute the month-15 change request | Drift eval 2/2; reconciliation delta 0; change-request timing recorded |
| **5** | Full eval harness → `REPORT.md`. Failure-case tests. Observability tables + ops page. CI. Railway deploy. `RUNBOOK.md`. README. 3-minute demo | All eight production-level criteria (§1.4) met |

**Revised by Addendum 1:** Project A is now **six** weeks. Weeks 1–4 as above; week 5 is the retrieval layer (A14–A18) and week 6 the exception resolution agent (A13). The original week-5 hardening work (eval harness, CI, deploy, runbook, README, demo) closes out week 6. See `docs/addendum1.md`.

### 2.8 Interview narrative — the five questions you must answer cold

1. **Why filter/validate before publishing rather than clean up in the report?** Because the report is the last place you find out. A bad row that reaches gold has already lied to two dashboards.
2. **Why is the LLM allowed to propose mappings but not apply them?** A mapping is a business decision with an audit trail. The LLM reads column names well; it has no authority. Wrong mappings are the highest-cost silent failure in integration work.
3. **Why a heuristic mapper at all if you have an LLM?** It's the floor, it's free, it runs when the LLM is down or wrong, and it gives you a baseline to measure the LLM's lift honestly.
4. **What happens when the source changes?** Fingerprint → diff → lineage walk → named breakage → publish blocked. Compare that to the old world: dashboard goes blank on Monday.
5. **What would you change before production?** Delta/Databricks for scale, a real M3 adapter via ION API or data lake, SSO, data contracts with source owners, alert routing, and retention policy for bronze.

**Interview keywords:** canonical schema · data profiling · schema fingerprinting · lineage · idempotency · exception queue · versioned mappings · semantic layer · reconciliation · deterministic validation · heuristic baseline · human confirmation.

## 4. Cross-Cutting

### 4.1 How the two projects tell one story

- A produces gold; B consumes it. A's metric dictionary is the same definition B's metric layer implements — one truth from ingest to brief.
- A's exception queue and B's action drafting both terminate in a human, never a system of record.
- A real-data path is one adapter swap in A; B is untouched. That's the "one foundation, many uses" slide, running.

### 4.2 README (Appendix B) — per repo, in this order

1. **Who is the customer?** Prairie Bend Foods, fictional; profile; why protein processing makes this hard (catch weight, shelf life, multi-plant).
2. **What was broken?** From the discovery brief.
3. **What did you build?** One paragraph + the architecture diagram.
4. **How does it work?** Walk one record (A) / one morning (B) through the system.
5. **How did you evaluate it?** Link `evals/REPORT.md`; the headline numbers; the failure analysis; what surprised you.
6. **What would you change before production?** From §2.8 / §3.8.

Then: Quickstart (`docker compose up`), Demo (3-minute video link), Runbook link, Design decisions log, Credits (reference repo for the DB-permissions pattern; Playbook source).

### 4.3 Claude Code working rhythm (both repos)

Session pattern that keeps you the decision-maker (Playbook §12.2):

1. **Start:** "Read CLAUDE.md and docs/discovery_brief.md. Summarise the current week's definition of done from docs/plan.md. Propose the next three tasks. Don't write code yet."
2. **Decide:** you pick, you adjust scope.
3. **Build:** one task per turn; tests before or with implementation.
4. **Verify:** `make test` / `make eval` output pasted back; failures analysed before fixes.
5. **Record:** append to `docs/decisions.md` — decision, alternatives, reason. This file becomes your interview script and your build-in-public material.
6. **Commit** with a message that states the decision, not just the change.

Kickoff prompts for session 1 of each repo are in Appendix A.

### 4.4 Build in public (IP-safe)

One post per week, tied to `docs/decisions.md`, never naming clients, never quoting real numbers:

- Week 1: the discovery brief and why "wants an AI agent" isn't a problem statement
- Week 2: heuristic mapper vs LLM mapper — the honest baseline
- Week 3: the exception queue, and why silent drops are the enemy
- Week 4: drift detection walking the lineage graph
- Week 5: the eval report, including what failed
- Weeks 6–10: same rhythm for B — the evidence pack, the validator that rejected the model, the replay results, the holiday decoy, the approval gate

Review anything that describes M3-specific detail before publishing.

### 4.5 Risks and mitigations

| Risk | Mitigation |
|---|---|
| Day-job workload compresses evenings | Week plans have hard "done" lines; A alone is a complete portfolio piece |
| Synthetic data feels toy-like | 18 months, 3 plants with real quirks, seeded faults and anomalies with causes — reviewers care about the quirks, not the row count |
| Over-building UI | Streamlit only; the API and the evals are the product |
| LLM cost creep | `ops.llm_calls` from day one; replay runs without LLM by default |
| Package/API drift (LangGraph, Anthropic SDK) | Verify docs at week start; pin versions after verification, not before |
| Public profile identifiable as a real company | Fictional name, "protein processor," no real numbers; review before publishing |

---
## Appendix A — Session-1 Kickoff Prompts for Claude Code

### Project A

```
Read CLAUDE.md fully, then docs/discovery_brief.md and docs/plan.md.

Context: this is week 1 of a five-week build. Definition of done for week 1:
- `make synth` generates the Prairie Bend Foods synthetic dataset (3 plants, 18 months,
  per-plant quirks as specified) and writes ground_truth/{mappings,faults,drift}.json
- `make profile` runs the profiler against all three sources and writes findings.md + JSON
- docker compose up brings up Postgres with bronze/silver/gold/ops schemas
- pytest green

Before writing any code: propose the repo layout, the synth generator's module structure,
and the ground-truth file schemas. List any assumption you're making about M3 table
shapes and flag it as "to confirm with M3 SME" rather than asserting it. Then stop and
wait for my go-ahead.
```


## Appendix B — What I could not verify (check before relying on it)

- Exact current package names/versions for LangGraph's Postgres checkpointer, the Anthropic Python SDK, Streamlit, FastAPI.
- Whether Jira Cloud still offers a free tier suitable for the `live` adapter test.
- Railway cron service behaviour and pricing at the time you deploy.
- Infor M3 physical field names for any table — canonical names are used throughout; confirm with an M3 SME before any real-data adapter is built.
- That "Prairie Bend Foods" is not a real company.
