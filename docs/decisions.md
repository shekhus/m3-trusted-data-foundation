# Decisions

Append-only. Format: **decision** · alternatives considered · reason. Newest at the bottom.

---

### D-001 · 2026-09-14 · Governing plan is plan v1 + Addendum 1; public copies only

- **Decision:** `docs/plan.md` (Project A sections of the master plan) and `docs/addendum1.md` govern the build. Project A is six weeks: weeks 1–4 as planned, week 5 retrieval (A14–A18), week 6 exception agent (A13). The repo holds public, scrubbed copies; engagement-specific material stays outside the repo.
- **Alternatives:** copy the master plan verbatim; keep the original five-week plan and treat retrieval/A13 as stretch.
- **Reason:** the repo is public, so nothing identifying a real engagement may enter git history. The addendum closes the retrieval and agentic gaps and says to defer A13 before retrieval if time is short.

### D-002 · 2026-09-14 · Dependency lower bounds resolved from PyPI

- **Decision:** `pyproject.toml` lists `>=` lower bounds at the versions resolved from PyPI on 2026-09-14 and installed in `.venv`. No lockfile yet.
- **Alternatives:** exact pins; a `uv.lock`.
- **Reason:** CLAUDE.md forbids pinning unverified versions. Lower bounds are verified-installed; exact pins wait until the stack stabilises (before the week-6 deploy). *Recorded retroactively from the `pyproject.toml` comment.*

### D-003 · 2026-09-14 · SQLite fallback when `DATABASE_URL` is unset

- **Decision:** `app/config.py` falls back to `sqlite:///data/m3tdf.sqlite` when `DATABASE_URL` is empty; Docker Compose always sets Postgres.
- **Alternatives:** Postgres-only (tests require a running database).
- **Reason:** Docker is not installed on the dev machine yet; unit tests must run without it. **Open question for task 3:** SQLite has no `bronze/silver/gold/ops` schemas — either `ATTACH` one file per schema or run migration tests against Postgres only. **Resolved by D-009: Postgres only.** *Recorded retroactively from `app/config.py`.*

### D-004 · 2026-09-15 · Make targets with `scripts/` equivalents

- **Decision:** keep the `Makefile` from the plan, but every target is a one-liner over `python -m pytest`, `python scripts/<name>.py`, or `docker compose`. `scripts/README.md` lists the no-make equivalent for each. Targets are added only when the command they run exists.
- **Alternatives:** `invoke` + `tasks.py` (plan §1.2 option); Make only.
- **Reason:** `make` is not installed on the Windows dev machine and CMD cannot run multi-line commands. `invoke` adds a dependency for no gain. Stub targets for unbuilt commands would be dead code.

### D-005 · 2026-09-15 · Container baseline: `python:3.12-slim`, `postgres:18-alpine`, deps via uv

- **Decision:** the app image is `python:3.12-slim`; dependencies install from `pyproject.toml` with `uv pip install --system -r pyproject.toml` (uv 0.11.3, same as the dev machine). Postgres is `postgres:18-alpine`. Compose runs `db` + `app` only; the Streamlit `portal` service is added in week 2 when `portal/` exists.
- **Alternatives:** `pip install .` (fails: no build backend, flat multi-package layout); Postgres 16/17.
- **Reason:** tags verified on Docker Hub on 2026-09-15; 18 is the newest major. Installing dependencies without packaging the app avoids adding a build backend just for Docker. ~~Not yet verified by a real `docker compose up`.~~ Verified 2026-09-15: image builds, `/healthz` healthy, Postgres 18.6 (see D-009).

### D-006 · 2026-09-15 · Repo hygiene test for engagement terms

- **Decision:** `tests/test_repo_hygiene.py` scans every text file in the repo (excluding `.venv`, `data`, `.git`) for banned engagement terms and fails on any whole-word match.
- **Alternatives:** manual review before publishing; a pre-commit hook.
- **Reason:** the planning documents started from a private plan. A test runs on every `make test` and in CI; a hook can be skipped. The banned list matches the synth generator's own guard.

### D-007 · 2026-09-15 · Import the synth generator as-is; keep its module layout

- **Decision:** `synth/` is the prepared generator (seed `20260912`), imported with its own modules (`generate`, `config`, `world`, `events`, `gold`, `export`, `bi_tools`, `knowledge`, `kb_questions`, `truth`) and probes under `synth/probes/`. CLAUDE.md's layout line was updated to match rather than renaming modules to `generator/quirks/faults/drift_events`. Its ground-truth tests live in `tests/test_synth.py`. `pyarrow>=25.0.1` added (verified on PyPI 2026-09-15). The RAG probe needs scikit-learn, which is deferred to week 5.
- **Alternatives:** rewrite the generator to the names in CLAUDE.md; keep it in a separate repo.
- **Reason:** its self-check, calibration and 40 integrity tests are the value; renaming adds risk and no information. Output was verified against the shipped sample: all 7 metric series equal, faults.json equal as JSON, sampled source CSVs byte-identical. Probe results match its README (fault recall 96.9%, precision 99.6%; A1 attribution top-1 correct).
- **Edits made on import (all verified output-neutral by regenerating and diffing — only timestamps differ):** UTF-8 + LF on every file write so Windows and Linux produce identical bytes; lint/type fixes (`cast`, annotations, loop-variable binding); scrubbed engagement references from `synth/README.md`; one shared banned-term list (`tests/banned_terms.py`) now scans all generated text, not three files.
- **Finding for week 3:** `fault_probe` reports V006 32/50 caught with 2 unexplained flags, where the generator README says 33/50. The ceiling in `faults.json` is 33. Investigate when V006 is implemented — likely a lot/expiry boundary (same-day expiry) rather than a data defect.

### D-008 · 2026-09-15 · Fix nondeterministic key order in faults.json; lock the answer key

- **Decision:** `truth._achievable_ceiling` iterated a `set`, so key order in `faults.json` changed with `PYTHONHASHSEED` (content identical). Now sorted. `data/` stays gitignored, but `tests/ground_truth.lock` commits sha256 hashes of 95 generated files (ground truth JSON except the timestamped manifest, source extracts, masters, report feeds, KB); `tests/test_ground_truth_lock.py` fails on any drift. Update with `python scripts/lock_ground_truth.py` only for intended changes, logged here.
- **Alternatives:** commit `data/ground_truth/` itself; trust the seed.
- **Reason:** CLAUDE.md says never rewrite ground truth; the seed alone did not guarantee that (this bug proved it). Hashes make any change loud without committing 18 MB. Verified: two runs with different `PYTHONHASHSEED` are byte-identical apart from timestamps, and the lock test fails when one file is altered. Parquet is excluded because its bytes depend on the pyarrow version.

### D-009 · 2026-09-15 · Migrations and database tests are Postgres-only; the app container migrates on start

- **Decision:** `db/migrate.py` applies numbered plain-SQL files from `db/migrations/` to Postgres and refuses any other backend. Each file runs in one transaction with its `public.schema_migrations` row (version, name, sha256 of the LF-normalised file); a session advisory lock serialises runners. It refuses an applied file whose checksum changed, an applied file missing from disk, and a new file numbered below the latest applied one. `0001` creates `bronze/silver/gold/ops`; `0002` creates the six ops tables named in CLAUDE.md with the plan §2.4 columns and constraints that encode the contracts (one confirmed mapping per source; one exception per batch × rule × row; a resolved exception needs who/when/what; `rule_id` matches `V###`). `bronze.raw_*`, `silver.*`, `gold.*` and the later ops tables (`tool_calls`, `runs`, `reconciliation`) arrive with their features. Database tests take a fresh throwaway database per test via the `pg_url` fixture and **skip** when Postgres is unreachable; `REQUIRE_POSTGRES=1` turns the skip into a failure (for CI). The Dockerfile runs `scripts/migrate.py` before uvicorn, so `docker compose up` alone yields the schemas (week-1 definition of done).
- **Alternatives:** one SQLite file per schema via `ATTACH` (tests run without Docker); Alembic; migrate only via `make migrate`.
- **Reason:** production is Postgres and the schema will use Postgres features (`jsonb`, partial unique indexes, regex checks, `gen_random_uuid`), so SQLite tests would test a different database. Alembic adds autogenerate machinery CLAUDE.md rules out (plain SQL, no ORM schema). Migrating on start is safe under the advisory lock. The SQLite fallback in `app/config.py` (D-003) stays for the API's `/healthz` only and is not migrated.
- **Verified:** 13 migration tests green against Postgres 18.6 in Compose, including a deliberately broken migration leaving no table and no record (this caught a real bug: SQLAlchemy did not track the transaction opened by the raw psycopg call, so the rollback was a no-op — fixed by beginning the transaction explicitly). From `docker compose down -v`, `docker compose up` applies 0001–0002 and a restart reports "database is up to date". With Postgres down the 7 database tests skip in ~6 s.

### D-010 · 2026-09-15 · Profiler infers from files and masters only; findings are committed and checked against the answer key

- **Decision:** `pipeline/profile.py` profiles every `data/sources/<source>/*.csv` (read as raw strings, nothing coerced) plus `data/master/`, and returns Pydantic `SourceProfile`s. Per column: dtype, blank %, distinct %, whitespace %, negatives, min/max, top value shapes (digit→9, letter→A), date format (ISO; slash dates resolved to US/DMY only when a part exceeds 12, otherwise `slash_ambiguous`; Excel serials by range 2000–2050), master match after trimming, and lb/kg from **both** header tokens and magnitude — median of `weight / (qty × item avg_lb_per_case)`, ≈1.0 lb, ≈0.4536 kg; disagreement is reported as a conflict, not resolved. Per source: header variants across files, the 1–2 column key with the fewest duplicates, exact duplicate rows, and plain-language findings. `make profile` writes `docs/findings.md` + `docs/findings.json` (deterministic; committed; a test fails when they are stale).
- **Alternatives:** read `ground_truth/mappings.json` for formats (cheating — the mapping eval would be circular); header-only unit inference (misses PLT-01/PLT-03, whose weight headers name no unit); prefer single-column keys (picks `Lot`, near-unique, over `(Order, Line)` at PLT-03); digit-normalised customer matching (line numbers and quantities also "match" bare customer numbers — left to the week-2 mapper).
- **Reason:** the profile is the mapper's input, so it must stand on the files alone; tests then score it against the answer key. **Verified against ground truth (tests/test_profile.py):** date format and weight unit for every mapped column at all three plants (magnitude alone agrees with the key); only the answer-key weight columns get a unit; both drift events show as header changes in the right month; trailing whitespace only on PLT-03 items; key duplicates = seeded V008 per plant (27/33/20); negative quantities = seeded V007 (16/14/5); the rare `LB` UOM shape = seeded V009 (28/13/14).
- **Finding:** `data/DATASET.md` and plan §1.3 say PLT-03 has ~0.5% ambient duplicate rows, but `synth/config.py` sets `ambient_dup_rate` to 0 for every plant; the profiler sees only the 20 seeded V008 duplicates at PLT-03. Generator left unchanged (the answer key is locked); the docs claim is wrong, not the data.

### D-011 · 2026-09-15 · Metric dictionary as versioned YAML compiled through a whitelisted expression language

- **Decision:** one file per metric version, `metrics/<metric>.v<version>.yaml`, validated by a Pydantic model (`extra="forbid"`; owner, changed_by, effective_from, status `current|legacy`, at most one current version per metric). Expressions follow the plan's example syntax (`issue_date <= confirmed_delivery_date`, `a if flag else b`, `sum(otif) / count(*)`). `metrics/compiler.py` parses them with Python's `ast` and renders SQL through a whitelist (comparisons, `in`, and/or/not, arithmetic, if/else, literals, names, and sum/avg/count/min/max in aggregations only); anything else is a `CompileError`. Each version compiles to one row-level view `gold.<metric>_v<version>_lines` holding the key, `metric_date`, dimensions and every input/rule as a boolean (missing input → FALSE); `aggregate_sql()` groups it by `day`/`month` and declared dimensions, with division as `x::double precision / NULLIF(y, 0)`. `apply()` checks every referenced name is a real column of the source table before creating views, so an unknown name (including a SQL function name) cannot reach Postgres. Compiled SQL is committed under `metrics/compiled/` with a staleness test. Shipped: `otif.v3` (governed), `otif.v2` (legacy, count basis for all items), `fill_rate.v1`. Migration `0003` adds `gold.fact_delivery` with **no** precomputed flags, so the YAML stays the only definition; `product_group` and `catch_weight_flag` are denormalised onto the fact because rules read them.
- **Alternatives:** Jinja SQL templates (definitions become SQL again, and injection-prone); dbt/MetricFlow (a framework for three metrics); `eval` of the expressions (unsafe); one YAML per metric with a version list (the legacy view needs the old definition as a first-class file).
- **Reason:** CLAUDE.md principle 5 — consumer views are compiled, never hand-written — needs a compiler that owners can read and reviewers can trust. **Verified (tests/test_metrics_compiler.py, Postgres 18):** loading the generator's 51,571-row gold fact, `otif.v3` by day × plant equals `data/metrics/daily_otif_plant` (lines exact, rates within 1e-9); `fill_rate.v1` by day equals `daily_otif_total`; by month, `otif.v3` equals `reconciliation.json` governed OTIF and `otif.v2` equals the legacy BI tool's OTIF for all 18 months (5 dp). Tool 2's differences (requested-date basis, 2% case tolerance, CONS excluded) are not a dictionary version — they belong to reconciliation attribution in week 4.

### D-012 · 2026-09-15 · The repo's `.env` overrides the shell environment

- **Decision:** `app/config.py` sets every `.env` pair into `os.environ` unconditionally (was `setdefault`). `.env` is gitignored and excluded from the image, so containers and CI take values from their real environment only.
- **Alternatives:** keep "env wins" and ask developers to unset conflicting variables; project-specific variable names (`M3_ANTHROPIC_API_KEY`).
- **Reason:** found on the dev machine: the shell already exported an unrelated, invalid `ANTHROPIC_API_KEY`, which silently shadowed the project key and produced a 401. A local `.env` is an explicit per-project choice and should not lose to an ambient variable. Renaming would break the SDK's default credential lookup. Test: `tests/test_config.py::test_dotenv_overrides_shell_environment`.

### D-013 · 2026-09-15 · Mappings bind to an exact header; heuristic mapper scored on answer-key and unseen headers

- **Decision:** a mapping is for one source **header** (ordered column list, `header_hash` = sha256 of it), not a whole source. Migration `0004` adds `header`/`header_hash`/`rejected_by`/`rejected_at` and moves the one-confirmed rule to `(source, header_hash)`. `pipeline/canonical.py` defines the 15-column canonical order line (names only; M3 physical names still to confirm with an SME) and the transform vocabulary. `pipeline/mapper/contract.py` is the single contract for heuristic, LLM, API and portal: every header column exactly once, known canonical names and transforms only, no canonical column twice. `pipeline/mapper/heuristic.py` scores pairs as 0.55 × name (concept Dice over an ERP-abbreviation vocabulary with run-together splitting) + 0.45 × profile evidence (dtype, unit, master match, distinctness; hard mismatches excluded), assigns greedily one-to-one, and derives transforms from the profile (date parser per detected format, `kg_to_lb`, `normalize_customer_no`, `trim`). API (`app/routers/mappings.py`, `X-API-Key` roles in `app/auth.py`): analyst/owner propose (one version per header variant, repeat proposals deduplicated) or submit a human-corrected mapping; **only owner** confirms or rejects; confirm is refused (422) while required canonical columns are unmapped and supersedes the previous confirmed mapping for that header; deciding a non-proposed version is 409. Compose mounts `./data` read-only into the app. Profiles are cached by file path/size/mtime.
- **Alternatives:** one mapping per source with optional columns (hides drift: PLT-02's `cust_no` and `customer_number` would both "map", so a rename could never block publish); Hungarian assignment (greedy already optimal on every case; revisit if it is not); fuzzy string distance instead of concepts (`ORDERNUM` vs `order_no` needs splitting, not edit distance).
- **Reason:** drift detection (week 4) needs "this header has no confirmed mapping" to be a fact in the database. **Measured (`make eval-mapping`):** answer-key headers 74/74 columns top-1 and 35/35 transforms across 5 headers — optimistic, because the vocabulary was written looking at these sources. The unseen-header case (`evals/cases/mapping_unseen_headers.yaml`, PLT-01 data under ERP-style names) scores **9/14 (64.3%)**, missing SalesDocument, ItemPosition, SoldToParty, ShippingPoint, CreatedOn — so required columns stay unmapped and confirm is blocked until a person or the LLM fills them. A test fails if that case reaches 100%, to catch the vocabulary being tuned to the eval. Failure cases (plan §2.6) in `tests/failure_cases/`: analyst and viewer get 403 on confirm/reject; an LLM-style answer naming a column not in the header is rejected by the contract.

### D-014 · 2026-09-15 · LLM mapper: one structured-output call behind llm/client.py, retry once, heuristic fallback

- **Decision:** `llm/client.py` is the only LLM entry point. `AnthropicBackend` sends one `messages.create` with `output_config.format` = a JSON schema (enums pin header column names, canonical names and transforms); the answer is parsed by the caller's Pydantic contract, not by the SDK, so fake and real backends share one path and a bad answer is visible as `invalid_output`. Every attempt is a row in `ops.llm_calls` (provider, model, sha256 prompt hash, tokens, latency, cost from `LLM_PRICE_*`, outcome, error). Refusal and `max_tokens` stops count as provider errors. `pipeline/mapper/llm.py` always runs the heuristic first, sends the canonical schema, transform meanings, column profiles (statistics plus at most 3 example values per column, never raw rows), the heuristic proposal and owner-confirmed mappings of *other* sources; an invalid answer is retried once with the validation error appended; a second invalid answer or any provider error returns the heuristic proposal with `needs_review`. The propose endpoint stores the heuristic proposal and, when the model answered validly, the LLM proposal as a separate version — the owner compares and confirms one. `make eval-mapping --llm` scores only headers the model actually answered and lists fallbacks separately, and passes no confirmed examples (the unseen-header case is PLT-01's data; examples would leak the answer). Model `claude-sonnet-5` ($2 / $10 per MTok, verified against the Claude API reference 2026-09-15), per plan §1.1.
- **Alternatives:** `client.messages.parse` with the Pydantic model (SDK-side parsing hides invalid outputs from the call log and ties tests to the SDK); tool use for JSON (structured outputs are the direct fit); letting the LLM proposal replace the heuristic one (loses the baseline the owner and the eval compare against); an agent loop (fails the justification test: the step count is fixed).
- **Reason:** principle 1 (AI proposes, never applies) and the convention that LLM output is contract-parsed, logged, retried at most once and falls back to the heuristic. **Verified:** 10 tests with a scripted backend cover ok, invalid-then-valid retry, hallucinated column twice → flagged heuristic fallback with exactly two calls, provider error → fallback with no retry, prompt contents, `ops.llm_calls` rows in Postgres, and the API storing heuristic + LLM versions. A live run on 2026-09-15 reached the API with the project key and logged 6 `error` calls: the account has no credit (`req_011Cf4iTqm38h4K3sYRTyQpF`). **LLM mapping accuracy is therefore not measured yet**; `tests/test_llm_mapper.py::test_live_provider_returns_a_contract_valid_mapping` runs with `RUN_LIVE_LLM=1` once credit exists.

### D-015 · 2026-09-15 · Bronze as generic JSON rows; silver derived from bronze through the header's confirmed mapping

- **Decision:** migration `0005` adds `bronze.raw_order_lines(batch_id, source_row, record jsonb)` — every source row as received, text values keyed by that file's header — and `silver.order_lines` with the 15 canonical columns typed, plus `source`, `mapping_version_id`, and `parse_errors jsonb` (canonical column → raw value). Silver rows reference their bronze row. `pipeline/ingest.py` makes one batch per file with idempotency key `file:<source>:<name>:<sha256>`: a replay returns the original batch and writes nothing. Bronze is always loaded; silver is built only when the file's exact header has a confirmed mapping, otherwise the batch is `blocked` naming the header, and a later run after confirmation maps the stored bronze rows (resume, same batch id). `pipeline/transforms.py` applies the mapping's transforms then parses to type; a non-blank value that fails becomes NULL with its raw value in `parse_errors`; blanks stay NULL without error (V001's job, week 3). The mapping contract now also refuses transforms that do not fit the column (a date needs exactly one parser; `kg_to_lb` only on weights; customer normalisation only on the customer number), so a mapping that cannot produce typed silver cannot be confirmed. `make silver SRC=plt01`.
- **Alternatives:** one bronze table per source with text columns (`bronze.raw_plt01`) — needs DDL on every drift event and per-source code; typing in bronze; silver straight from the file (then a blocked batch would have to re-read the file, and bronze would not be the single record of what arrived).
- **Reason:** principle 3 (nothing silently dropped or changed; originals kept) and the plan's "safe re-run". **Verified (`tests/test_silver.py`, Postgres):** all 54 files of the three plants ingest with bronze rows = CSV rows; replaying a file adds no batch and no rows; PLT-01's `lot_no` header blocks its three files until that mapping is confirmed, then the same batches resume to `mapped`; for every one of the >50,000 gold lines without a seeded fault, silver equals the generator's clean gold — customer, item, plant, UOM, all four dates (ISO, US, Excel serial), quantities exactly, weights within 0.023 lb (PLT-02 is written in kg rounded to 0.01) — with zero parse errors, and the only NULL invoiced weights are PLT-02's documented ~1% missing-weight quirk. On the dev database, `make silver SRC=plt01` mapped 15 files (17,994 rows, 0 parse errors); the 3 files with the new `lot_no` header wait for an owner to confirm that header's mapping.

### D-016 · 2026-09-15 · Portal is a thin Streamlit client of the API; uploads live outside the generated data

- **Decision:** `portal/app.py` (tabs: Sources & upload, Findings, Mappings, Batches) calls the API only, through `portal/api.py`, with the key the user types; it holds no permission logic, so an analyst pressing Confirm sees the API's 403. New endpoints in `app/routers/sources.py`: `GET /sources` (generated + uploaded, with batch status counts), `GET /sources/{source}/profile`, `POST /sources/{source}/files?name=` (raw `text/csv` body; lowercase source name, plain `.csv` name, ≤20 MB, UTF-8 header with ≥2 distinct columns; identical re-upload is a no-op, different content under the same name is 409), `POST /sources/{source}/silver`, `GET /sources/{source}/batches`. Uploads are written to `data/uploads/<source>/`, never into `data/sources/`. The mappings editor lets a person change canonical targets and transforms and submit a `human` version; confirm and reject stay owner-only on the server. Compose adds a `portal` service on :8501 and mounts `./data/uploads` writable into the app (the rest of `data/` stays read-only). mypy now checks `portal/`.
- **Alternatives:** `UploadFile` multipart (needs `python-multipart`, not a declared dependency); the portal reading Postgres directly (a second permission path); uploading into `data/sources/` (breaks `tests/ground_truth.lock` and mixes real uploads with the answer-key data).
- **Reason:** principle 7 (permissions are server-side) and plan §1.1 (Streamlit, honest internal console). **Verified:** API tests upload a new source, profile it, propose, owner-confirm and build silver end to end on a temp data dir; upload validation cases (path traversal, non-CSV, duplicate or blank header, non-UTF-8, 409, viewer 403); the portal client surfaces an analyst's 403 and an unreachable API as messages, not exceptions; Streamlit `AppTest` runs the portal with no key and with a dead API. Live on Docker: portal health 200, the portal script rendered all tabs against the running API as owner (3 sources, PLT-01 findings, header 1 confirmed, header 2 awaiting confirmation) with no exception, and an upload through the container wrote to the host's `data/uploads` (test file removed).

### D-017 · 2026-09-15 · Groq (openai/gpt-oss-120b, strict JSON schema) as the configured provider; LLM lift measured

- **Decision:** add `GroqBackend` to `llm/client.py` next to `AnthropicBackend`; `LLM_PROVIDER=groq` selects it. It calls Groq's OpenAI-compatible chat completions with `httpx` (already a dependency; no extra SDK pinned) and `response_format = {type: json_schema, json_schema: {name, strict: true, schema}}` — constrained decoding, supported on `openai/gpt-oss-120b` per Groq's structured-outputs docs (read 2026-09-15). Non-2xx and any `finish_reason` other than `stop` are provider errors. A 429 with `retry-after` ≤ 30 s is waited out once (transport, not a second answer); longer limits fail fast. The local `.env` uses `LLM_MODEL=openai/gpt-oss-120b`, `LLM_PRICE_IN/OUT_PER_MTOK=0.15/0.60` (Groq models page, 2026-09-15); `GROQ_API_KEY` is in `.env.example` and passed through Compose. The Anthropic backend stays and is selectable. `scripts/eval_mapping.py` gains `--pace` (seconds between LLM headers, for tokens-per-minute limits) and `--case`.
- **Alternatives:** the `groq` SDK (a new unverified dependency for one POST); JSON mode without a schema (no guarantee of shape); `openai/gpt-oss-20b` (cheaper; not measured); waiting indefinitely on 429 (hides a daily cap as a hang).
- **Reason:** the Anthropic account has no credit (D-014), and the plan requires the LLM-assisted mapping accuracy to be measured, not assumed. **Measured 2026-09-15 (`--llm --pace 60`, no confirmed examples):** answer-key headers 5/5, 74/74 columns, 35/35 transforms — equal to the heuristic. **Unseen ERP-style headers: 14/14 columns and 7/7 transforms in 3 of 3 runs, against the heuristic's 9/14 and 6/7** — the LLM mapped SalesDocument, ItemPosition, SoldToParty, ShippingPoint and CreatedOn that the heuristic left unmapped. One of the 13 successful attempts first returned a contract-invalid answer and was recovered by the single retry. Groq spend for all runs ≈ $0.02 (`ops.llm_calls`). Caveats: one unseen header of 14 columns is a small sample, and the free tier's 8,000 tokens/minute makes an unpaced run fall back (the eval reports those as fallbacks, never as accuracy). The plan target (top-1 ≥ 90% LLM-assisted) is met on both cases.

### D-018 · 2026-09-15 · Rules V001–V011 as deterministic DataFrame checks over silver; exceptions keyed by source row; V006 ceiling corrected from the data

- **Decision:** `rules/` holds one class per rule (`id`, `name`, `severity`, `owner`, `message`, `suggested_fix`, `check(df, ctx) -> Violations` with per-row evidence), registered in `rules/__init__.py`. `pipeline/validate.py` runs every rule on a batch's silver rows after silver is built, inserts `ops.exceptions` idempotently (migration `0006`: identity is `(batch, rule, source_row)` with a foreign key to the bronze row — `(order_no, line_no)` is not unique in a batch, which is V008's finding — plus `source` and `details` evidence), resolves open exceptions that no longer apply on re-validation as `system:revalidation` (never deletes), and records `rows_validated` and `rules_run` on the batch (status `validated`). Definitions: V001 required fields blank (a value that failed to parse belongs to V010, not V001); V002 issue before order, or confirmed > 30 days before requested; V003 invoiced weight < 0 or > 105% of ordered; V004/V005 not in customer/item master; V006 only where the header maps `lot_no`: blank lot, lot not in lot master, or shipped after expiry; V007 negative quantity; V008 repeated (order, line) in the batch — first kept, repeats queued; V009 **warn**, UOM ≠ item master basis, with a kg-magnitude ratio as evidence and a suggested fix, **not auto-applied**; V010 any parse error; V011 **warn**, invoiced with no positive ordered quantity. Owners: source data problems → `source:<plant>`, V004 → `master_data:customers`, V005 → `master_data:items`, V006 → `quality`. `evals/exception_eval.py` + `make eval-exceptions` score recall per rule and precision by row (answer-key definition) and by (row, rule), naming unloaded sources and every unexplained exception.
- **Alternatives:** row-level Python loops (2m41s on the full load; vectorised: 25 s for 54 files); one exception per row with a rule list (loses per-rule ownership and resolution); applying V009's kg→lb fix in the pipeline as plan §2.4 suggests (conflicts with principle 1 and the A13 policy gate: an auto-fix needs the allowlist and an owner — deferred to the exception workflow); taking `achievable_ceiling` from the answer key as the V006 denominator.
- **Reason:** principles 2 and 3. **Found (resolves the D-007 V006 question):** the answer key counts a V006 fault detectable when it *shipped* on/after 2026-06-01, but extracts are split by *order* month; `SO0014109|5` was ordered 2026-05-21 and shipped 2026-06-02, so it sits in `plt-01_orderlines_2026-05.csv`, which has no lot column. True detectable V006 = **32** (computed from loaded mappings), key says 33; `faults.json` left untouched. The 2 "unexplained" V006 flags (`SO0009044|1` lot L02251226229, `SO0012682|1` lot L03260409400) are real: the lot master's expiry precedes the issue date in the generator's clean data. **Measured (full load, all 54 files, heuristic mappings):** recall **100%** on every rule (562/562 detectable); row precision **99.6%** (2 of 564 rows, the genuine expired lots); pair precision 96.9% (the 16 V011 warnings sit on V007's negated-quantity rows); 580 exceptions, 509 of 51,651 silver rows carry a blocking one. Quirks raise nothing: PLT-02's missing weights, kg weights, date formats, trailing spaces, PLT-01's lot-less months. Plan target (≥ 95% / ≥ 95%) met. Tests: a seeded shape per rule, quirk cases, re-validation idempotence and system resolution, coverage of all rules on all rows of all 54 batches.

### D-019 · 2026-09-15 · Idempotent POST /ingest/{source}: request keys over content-keyed batches; adapters; stale = extract age

- **Decision:** `POST /ingest/{source}` (analyst/owner) requires `Idempotency-Key` (8–200 of `A-Za-z0-9_.:-`) and optionally `{"files": [...]}`. Migration `0007` adds `ops.ingest_requests` (key, source, sha256 of `{source, files}`, requester, status `in_progress|completed|failed`, attempts, stored response, error). `pipeline/idempotency.claim()` decides under a row lock: new key → run; completed + same request → return the stored response verbatim with `Idempotent-Replayed: true`; same key + different request → 422; in progress → 409 `Retry-After: 5` (a lease of 15 minutes lets a new call take over from a dead worker); failed → run again (`attempts` + 1). Underneath, every file's batch keeps its own key `file:<source>:<name>:<sha256>`, so neither a retry nor a new key can duplicate bronze, silver or exceptions. `sources/` adds the adapter protocol (`sources()`, `discover()`, `fetch()` → `SourceUnavailable(retry_after)`) with a filesystem adapter over `data/sources` and `data/uploads`; the router depends on it, so a real M3 adapter slots in. Source unreachable → 503 with `Retry-After`, a `failed` batch naming the file and reason, the request marked failed. **Stale:** an extract produced (adapter `produced_at`, file mtime for files) more than `SOURCE_SLA_HOURS` (72) before ingest is accepted with `ops.batches.stale = true` and listed in the response; publish (week 4) refuses stale batches. `POST /sources/{source}/silver` is removed; the portal's ingest button and `make ingest SRC=` (fresh key, `--key` to replay) use `/ingest`.
- **Alternatives:** file-level idempotency only (a client retry after a timeout cannot get its original answer, and a key cannot be refused for a different request); a Redis lock (another service for one table's job); staleness as lateness after the period end, as `.env.example` first said (every historical backfill would be stale and publish would never run — comment corrected); failing the whole request on a stale file (the plan says accept and flag).
- **Reason:** plan A7 and §2.6 (source unreachable, duplicate ingest, stale source). **Verified:** tests in `tests/test_ingest_api.py` and `tests/failure_cases/test_ingest_failure_cases.py` — the same key 3× returns an identical body with the replay header and leaves batches/bronze/silver/exceptions counts unchanged; a new key over the same files adds nothing (every file `replayed`); key reuse with other files → 422 and the original still replays; an unreachable second file → 503 `Retry-After: 120`, one `failed` batch, request failed, then the same key retries to `completed` with `attempts = 2`, the first file replayed rather than reloaded; a file aged 5 days is flagged stale while the fresh one is not; a key held in progress → 409 and no batch. **Live on Docker:** `scripts/ingest.py plt01 --key live-replay-demo-0001` three times → 18 validated each time, database counts 18 batches / 21,634 bronze / 21,634 silver / 222 exceptions before and after every call, one `completed` request with 1 attempt. Note: synthetic extracts carry their generation time as mtime, so they turn stale 72 h after `make synth`; regenerate or raise the SLA for demos.

### D-020 · 2026-09-15 · Lineage recorded at confirm; impact derived from compiled metrics, not listed

- **Decision:** `pipeline/lineage.py` writes one `ops.lineage` row per mapped column of a mapping version — source column → `silver.order_lines` column → `gold.fact_delivery` column, with the column's transforms (migration `0008` adds `silver_table` and `transforms`) — inside the confirm transaction, so a confirmed mapping never exists without lineage. Rows stay when a version is superseded; queries show confirmed lineage by default and history with `include_superseded`. Silver → gold is identity by name (`GOLD_COLUMNS`); publish (week 4) is the only place allowed to change that map. `impact(source, source_col)` returns the gold columns fed and the compiled metric views that read them, computed from `metrics/*.yaml` through the compiler's `source_columns` (key, period, dimensions, referenced columns), so a new metric is covered without editing lineage code. `upstream(gold_table, gold_col)` is the reverse. Read-only `GET /lineage/impact` (404 when the column feeds nothing confirmed) and `GET /lineage/upstream` for any role. `backfill()` runs after migrations in `scripts/migrate.py` (and therefore on container start), and silver builds also record lineage, covering mappings confirmed before lineage existed. `.flake8` added so editor linting uses the project's 110-character limit.
- **Alternatives:** lineage written at publish (week 4) — drift detection and the exception agent need it before anything is published; a hand-maintained report→column list (drifts from the metric YAML the moment a metric changes); a SQL function in a migration for the backfill (a second definition of the column map).
- **Reason:** plan A8 — drift must name exactly which report a broken source column affects. **Verified (`tests/test_lineage.py`, real PLT-02 files either side of the rename):** confirming both headers records 15 lineage rows each, with `kg_to_lb` on the weight columns; impact of `plt02.cust_no` → `gold.fact_delivery.customer_no`, all three views (it is a dimension of every metric), lineage from v1 with `normalize_customer_no`; `WT_SHIP_KG` → only `fill_rate_v1` and `otif_v3` (v2 counts cases); `LOTID` → `lot_no`, no view; upstream of `customer_no` → `cust_no` (v1) and `customer_number` (v2); a human correction supersedes v1, current upstream shows only v2 and history shows both; backfill restores 30 rows and is idempotent. **Live:** container start applied 0008 and backfilled 29 rows for PLT-01's two confirmed mappings; `/lineage/impact?source=plt01&source_col=inv_wt` names `invoiced_weight_lb` and the two weight-based views.

### D-021 · 2026-09-15 · Exception workflow: assign (analyst/owner), resolve with a publishable kind (owner only), append-only events

- **Decision:** `app/routers/exceptions.py` — `GET /exceptions` (filters: source, status, severity, rule, owner, batch; paging), `GET /exceptions/summary`, `GET /exceptions/{id}` (evidence plus the bronze record as received, the silver row and the event history), `POST /exceptions/{id}/assign` (analyst/owner; open or assigned only), `POST /exceptions/{id}/resolve` (**owner only**; resolved is final → 409), `POST /batches/{id}/revalidate` (analyst/owner). Migration `0009`: `resolution_kind` ∈ `accept` (publish the row as sent) / `exclude` (never publish) / `fixed_at_source` (a corrected extract will replace it) / `no_longer_violated` (system only — the API refuses it), required exactly when resolved; `assigned_by`/`assigned_at`; `ops.exception_events` (assigned, resolved, auto_resolved) written in the same transaction as each change, including the system closures in `validate_batch`. A resolution needs a kind and at least 10 characters of reasoning. Resolving never edits bronze or silver. The portal gets an Exceptions tab: counts, filters, the queue, evidence and raw record side by side, assign/resolve forms (the server decides who may), and a re-run button.
- **Alternatives:** free-text resolutions only (publish could not act on them; week 4 would have to parse prose); letting analysts resolve warnings (principle 7 says owner); reopening resolved exceptions (not in plan A6 — a changed mind is a new decision, raised again on the next corrected extract); storing history as columns (loses repeated assignments; the week-6 agent writes events too).
- **Reason:** plan A6 (assign / resolve / re-run, nothing dropped silently) and principles 1, 3, 7; the kinds are what publish (week 4) needs to decide which rows reach gold. **Verified (`tests/test_exceptions_api.py`, `tests/failure_cases/test_exception_permissions.py`, on a real PLT-01 ingest):** filters and paging agree with totals; V004 exceptions route to `master_data:customers`; detail returns the raw record whose order number matches the row key and the silver customer that failed; assign then owner-resolve records `exclude` with both events in order; re-resolve and assign-after-resolve → 409; short reasons, unknown kinds and the system-only kind → 422; analyst and viewer → 403 on resolve with no event written; viewer → 403 on assign; after a corrected quantity, revalidate closes exactly that V007 exception as `no_longer_violated` with an `auto_resolved` event, keeps a person's `accept`, and a second run closes nothing. **Live:** migration 0009 applied on container start; the portal's Exceptions tab rendered against the running API with PLT-01's queue (187 open blocking of 222 open; by owner: source:plt01 184, master_data:customers 20, master_data:items 16, quality 2).

### D-022 · 2026-09-15 · Drift = change of a coarse per-file fingerprint; one alert per new shape; resolution needs a confirmed mapping

- **Decision:** `pipeline/drift.py` fingerprints every new batch right after its bronze load: sha256 of the sorted `(column, dtype, null_bucket, pattern_class)` tuples of that one file (profiled with the same profiler, now callable on in-memory frames via `profile_frames`). `null_bucket` is sparse (≤ 5% blank) / partial (≤ 50%) / mostly; `pattern_class` is the detected date format, else a dominant (≥ 90%) value shape for strings, else the dtype. A fingerprint different from the source's previous one raises one `ops.drift_alerts` row per new shape (migration `0010`, unique on source + new fingerprint), with evidence: removed, added and retyped columns, likely renames (a removed and an added column with identical dtype/bucket/pattern), and — through lineage — the canonical and gold columns and compiled views a removed or retyped column feeds; an added column is named with the canonical column the heuristic mapper proposes for it. The ingest response lists open alerts per batch. `GET /drift/alerts` (any role); `POST /drift/alerts/{id}/resolve` is owner-only and refused (409) until the alert's new header has a confirmed mapping. Publish (task 14) refuses batches whose shape has an open alert. `drift.backfill()` runs after migrations and fingerprints older batches from their bronze rows in arrival order. `make eval-drift` scores against `drift.json`, marking events of unloaded sources "not scored". Portal Batches tab lists open alerts with a resolve form.
- **Alternatives:** the header hash alone (misses a date-format or type change under an unchanged header, e.g. a plant switching to DD/MM); a fine null bucket or distinct-count in the fingerprint (monthly blanks from seeded V001 faults would read as drift); an alert per batch (six PLT-02 files in the new shape would raise six); letting any role acknowledge an alert (principle 7).
- **Reason:** plan A9 and §2.6 "Schema drift → publish blocked; alert names mapping + affected reports". **Measured before building:** on all 54 files the fingerprint changes exactly twice — PLT-02 at `2026-03` (`cust_no` → `customer_number`) and PLT-01 at `2026-06` (`lot_no` added) — and never from monthly noise. **Verified (`tests/test_drift.py`, full load):** drift eval 2/2 detected, 2/2 named, 0 false alerts; D1 message "'cust_no' appears renamed to 'customer_number'. Mapping for plt02 source column 'cust_no' is broken; gold.fact_delivery.customer_no and views … are affected"; D2 "New source column 'lot_no' detected on plt01 with no mapping (likely canonical column lot_no)"; 54 fingerprints, 5 distinct shapes; backfill from bronze reproduces the same digests and raises nothing new; API: resolve refused before the new header is confirmed, analyst 403, owner 200, second resolve 409, and a later ingest of the same shape reports no open alert. **Live:** container start applied 0010 and backfilled 18 PLT-01 batches, raising the real D2 alert on `plt-01_orderlines_2026-06.csv`; `make eval-drift` → D2 detected and named, D1 not scored (PLT-02 not loaded in the dev DB), 0 false alerts.

### D-023 · 2026-09-15 · Publish silver → gold per batch: refuse stale or drifted batches, hold rows by exception state, owner only

- **Decision:** `pipeline/publish.py` publishes one batch per transaction. A batch is **refused** (recorded with reasons, gold untouched) unless it is `validated`/`published`, not `stale`, and its fingerprint has no open drift alert. In a publishable batch a row is **held** when it has an open/assigned blocking exception, any exception resolved `exclude` or `fixed_at_source`, a blank required canonical field (an `accept` cannot publish a blank customer), or repeats an order line already taken from the batch; warnings never hold. Held rows are counted by reason. Publishing deletes the batch's gold rows and upserts the eligible ones; on an order line already in gold from another batch, the later-received batch wins. Product group and catch-weight flag come from the item master, production/expiry dates from the lot master; `customer_po` and `order_type` stay NULL (no plant extract carries them). Re-validating a published batch **withdraws** its gold rows (`ops.publishes` status `withdrawn`) until it is published again. Migration `0011`: gold nullability follows the canonical `required` flags (weights, invoiced qty, UOM, lot, product group, catch-weight flag, order type nullable), gold rows carry `batch_id`, `source`, `source_row`, `published_at`; `ops.publishes` records every published/refused/withdrawn event with rows published and held by reason. `POST /publish/{source}` is **owner only**; `GET /publish/history` any role. Portal Batches tab has a publish button that shows refusals and held rows.
- **Alternatives:** keep gold NOT NULL everywhere (PLT-02's ~1% missing weights and every order type would block publish); enrich order type from the legacy BI tool's report feed (makes a consumer of gold a source of gold); leave gold rows in place on re-validation (gold could keep a row that a new exception picture holds back); analyst-triggered publish (gold is what reports read; principle 7 keeps governed acts with the owner).
- **Reason:** plan §2.4 publish and §2.6 (stale → publish blocked; schema drift → publish blocked). **Verified (`tests/test_publish.py`, full three-plant load):** with both drift alerts open, exactly PLT-01's 2026-06..08 and PLT-02's 2026-03..08 batches are refused, each for "open drift alert"; published rows = eligible silver rows = gold rows; every gold row has a catch-weight flag, lot rows have an expiry date, all rows carry provenance; after resolving the alerts all 54 batches publish, gold = silver rows − rows with an open blocking exception, one row per order line, and re-publishing changes nothing; governed OTIF v3 per month on published gold stays within 0.01 of `reconciliation.json` for all 19 months (the gap is held rows and PLT-02's missing weights); resolving a V004 as `accept` publishes that row and a V009 warning as `exclude` removes it; re-validating a published batch withdraws its rows and republishing restores the same count. API: analyst 403; a stale extract refused with "extract is stale (older than the source SLA)" while the fresh one publishes; history lists both. **Live:** migration 0011 applied on container start; nothing published in the dev DB — publishing is the owner's call, and PLT-01's June–August batches would be refused until the open D2 alert is resolved.

### D-024 · 2026-09-15 · Consumer views compiled from a catalogue; BI tool gaps explained by causes identified from each tool's own export

- **Decision:** `metrics/consumers/consumers.yaml` declares the consumer views (two current OTIF views — ops month × plant, sales day × plant × customer — plus the legacy v2 view, and two fill-rate views) and the pairs that must reconcile; `compile_consumers` validates it (the metric version exists, the legacy flag matches the metric status, reference definitions cannot be consumed, numerator/denominator aggregations exist, the grain is reachable from both views) and compiles `gold.consumer_*` views from `aggregate_sql`. None is hand-written. The competing BI definitions are added as `status: reference` metrics (`otif_date_basis.v1`, `otif_case_tolerance.v1`), so every cause is sized by a compiled definition. `pipeline/reconcile.py` (migration `0012`: `ops.reconciliation_runs`, `ops.reconciliation`, `ops.reconciliation_tools`; `POST /reconcile` analyst/owner, `GET /reconcile/latest` any role; `make reconcile`; a Reconciliation tab in the portal) (1) rolls each pair up to month × plant by summing numerator and denominator and marks a period red if the rates differ by more than 1e-9, recording the legacy view's gap alongside; (2) for each BI tool it works out which definition the tool used **from the tool's export alone**: line-level on-time and in-full agreement for the legacy report, and for the dashboard all 12 combinations of date basis × in-full basis × consignment scope, keeping the combinations that best reproduce the export. Causes shared by every best match are *identified*; causes only some of them imply are *undetermined*, still sized but reported as unconfirmed. Each cause is a single-switch delta from governed, reported side by side and never summed into the gap. The dominant cause is chosen among identified causes only. Order type is read from the legacy report feed for the scope check only and never written to gold (D-023). Lineage impact now also names the consumer views built on an affected metric view.
- **Alternatives:** reading the tools' definitions from `reconciliation.json` (circular: the eval would score the answer key against itself); a single best guess when definitions tie (a 2% case tolerance never changes a verdict in this data, so a guess would be reported as a finding it cannot support); attributing the gap as additive shares (switches interact, so the shares would not be real); hand-written consumer SQL (principle 5).
- **Reason:** plan A10: two consumer views agree, and the BI tool gap is explained by named causes. **Verified (`tests/test_reconcile.py`, `evals/reconciliation_eval.py`):** on the generator's gold, the consumer views are green on all 114 comparisons (19 months × 3 plants × OTIF and fill rate). The legacy report is identified as `weight_basis` (dominant `weight_basis`). The dashboard is identified as `date_basis`, `scope_filter`, `weight_basis` (dominant `date_basis`). `case_tolerance` is undetermined for both. Every month with a gap above 0.001 is flagged, the worst contribution error against the key is 0.0, and the consignment lines dropped match exactly. The legacy view's gap is reported (57 OTIF rows). On gold we published ourselves from the three plants, the views also agree (114 green) and the dominant causes are the same. Live dev DB: nothing published yet, so the run returns `no_data`.

### D-025 · 2026-09-15 · lot_no change request absorbed by a field dictionary bump + V012; history queued as "not covered" warnings, not blocked

- **Decision:** A versioned **field dictionary** (`metrics/fields/order_line.v{n}.yaml`, loaded and validated by `metrics/fields.py`; exactly one version is current) says which canonical fields a row must carry, and from when. v1 is the baseline: the ten fields V001 used to hardcode, so V001 now reads them from the dictionary with unchanged behaviour. v2 is the change request CR-001: `lot_no` is required from 2026-06-01, compared against `issue_date`, owner `quality`. New rule **V012** checks every dated requirement. A row dated on or after the date that lacks the field is **blocked**; a column the mapping does not supply counts as missing. A row dated before it is queued as a **warning** marked "not covered", with a fix that says so. Rows with no date are left to V001/V010. To support this, `Violations` can carry per-row severities and owners, and the engine uses them. `make revalidate [SRC]` re-runs the rules on existing batches: new exceptions are queued, nothing is deleted, and published batches are withdrawn until they are published again. The loader rejects any requirement that cannot be checked: unknown column, a non-date `by`, a dated requirement missing its owner or fix, or an undated requirement on a column a mapping may leave unmapped. No report SQL, compiled view, migration, mapping, or silver/gold schema changed.
- **Alternatives:** block the historical rows too (15 months of PLT-01 history, 17,695 lines, would leave gold for a field the extract never carried: a retroactive requirement presented as a data defect; addendum §2.5 says the correct answer for that period is "not covered"); skip the historical rows silently (breaks "queued, not dropped", plan §2.6); bump OTIF to v4 to carry the requirement (a traceability rule is not part of a metric definition, and every consumer view would change); hardcode `2026-06-01` in the rule (the next requirement would be a code change, not a dictionary bump).
- **Reason:** plan A11 and §2.5 "Change absorption", §2.6 "Mid-project new required field", drift.json D2. **Verified:**
  - **Expected exceptions** are computed from the raw extract in bronze plus the answer key, not from V012 (`evals/change_request_eval.py`, case `evals/cases/change_request_lot_no.yaml`). On the full three-plant load the match is exact: PLT-01's no-lot header gives 17,695 warnings for lines issued before 2026-06-01 and 299 blocks for May orders issued from 1 June, with 0 missing, 0 unexpected and 0 wrong severity. Bronze rows equal silver rows (21,634), so nothing was dropped. PLT-02 and PLT-03 raise 0 (they always send a lot).
  - **Publish** holds the V012-blocked lines and still publishes the history marked "not covered" (`tests/test_field_dictionary.py`).
  - **Seeded-fault eval:** V012 is excluded from it because it is not a seeded fault family. Recall and precision are unchanged.
  - **Live dev DB:** `make revalidate SRC=plt01` then `make eval-change-request` → PASS with the same counts.
  - **Change absorption, reported honestly:** about 17 minutes of wall-clock time from the start of the task to the live PASS (agent-driven, including three test runs). 16 files touched, 8 modified and 8 new:
    - **The change itself:** 1 file (`order_line.v2.yaml`).
    - **One-time capability:** 6 files. `metrics/fields.py`, v1 yaml, `rules/base.py`, `rules/completeness.py`, `rules/__init__.py`, `pipeline/validate.py`.
    - **Measurement and operations:** 7 files. The eval, its case and script, `revalidate.py`, `exception_eval.py`, Makefile, scripts README.
    - **Tests:** 2 files.
    - **Report SQL edited:** 0.
  - **Next time:** a field that becomes required from a date is one YAML file plus `make revalidate`.

